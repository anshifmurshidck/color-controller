# color-controller
npx create-react-app color-controller
cd color-controller
# Replace src/App.jsx with the code above
git init
git add .
git commit -m "Color Controller - React mini task"
git remote add origin https://github.com/YOUR_USERNAME/color-controller.git
git push -u origin main
